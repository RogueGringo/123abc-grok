# Dual-gate handoff + known-solutions science path (CPU).
# pip install -r requirements-handoff.txt
numpy>=1.24
pytest>=7.0
fpdf2>=2.7
# Optional commercial open metric:
# biopython>=1.81
# Optional HF expand (CPSea2 demo metadata only):
# huggingface_hub>=0.20
